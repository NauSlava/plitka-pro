# cog.yaml
# Version: v4.3.5 - CRITICAL EXPANDABLE_SEGMENT FIX & VAE CPU FALLBACK
build:
  gpu: true
  python_version: "3.11"
  python_requirements: "requirements.txt"
  cog_runtime: true
  run:
    - mkdir -p /src/model_files
    - mkdir -p /src/refs
predict: "predict.py:OptimizedPredictor"