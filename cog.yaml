# cog.yaml
# Version: v4.3.3 - CRITICAL VAE DECODE FIXES & Accelerate Memory Sync
build:
  gpu: true
  python_version: "3.11"
  python_requirements: "requirements.txt"
  cog_runtime: true
  run:
    - mkdir -p /src/model_files
    - mkdir -p /src/refs
predict: "predict.py:OptimizedPredictor"